import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sa.ibr7h.miqati',
  appName: 'ميقاتي',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
